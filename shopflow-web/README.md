# shopflow-web

React + TypeScript UI for catalog, order creation/detail, and shipping. Every
application request uses relative `/api` URLs, which are proxied only to
`shopflow-gateway`.

```bash
npm ci
npm test
```
